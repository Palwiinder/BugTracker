@model Mvc2.Models.EditViewModel
@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>
<form method="post">
    <div class="form-group">
        @Html.LabelFor(m => m.DisplayName, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.DisplayName, new { @class = "form-control" })
        </div>
    </div>
</form>